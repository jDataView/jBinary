build:common:
  - newer:lintspaces
  - newer:jshint:grunt
  - newer:replace
  - newer:6to5

build:browser:
  - build:common
  - pure_cjs
  - newer:uglify:browser

build:node:
  - build:common
  - newer:uglify:node

default:
  - node
  - browser

browser:
  - build:browser
  - test:browser
  - compare_size:browser

node:
  - build:common
  - test:node
  - newer:uglify:node

live:
  - karma:watch:start
  - watch
